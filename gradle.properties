org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Gradle 8.14 cannot run on JDK 25, which is what JAVA_HOME points at on this machine.
# Delete this line if your JAVA_HOME already is a JDK 21.
org.gradle.java.home=D:/Minecraft/jdks/jdk21

# Pinned to the exact versions the target client runs (see logs/latest.log).
minecraft_version=1.21.4
yarn_mappings=1.21.4+build.8
loader_version=0.16.14
fabric_version=0.119.3+1.21.4

mod_version=0.3.0
maven_group=com.cestats
archives_base_name=cestats
